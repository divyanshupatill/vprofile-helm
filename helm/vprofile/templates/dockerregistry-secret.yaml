{{- if .Values.dockerregistry.enabled }}
{{- $auth := printf "%s:%s" .Values.dockerregistry.username .Values.dockerregistry.password | b64enc -}}
{{- $config := dict "auths" (dict .Values.dockerregistry.server (dict "username" .Values.dockerregistry.username "password" .Values.dockerregistry.password "email" .Values.dockerregistry.email "auth" $auth)) | toJson | b64enc -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-docker-registry
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: {{ $config | quote }}
{{- end }}
